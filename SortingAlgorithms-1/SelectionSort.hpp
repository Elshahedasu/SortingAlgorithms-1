#ifndef SELECTIONSORT_HPP
#define SELECTIONSORT_HPP

#include "Vector.hpp"
#include <iostream>

// Selection Sort function template
template <typename T>
void SelectionSort(Vector<T>& vec, bool ascending = true, bool stepbystep = false);

#endif
